Small status pill for state labels (open/closed, live, pending) and counts.

```jsx
<Badge variant="success">Active</Badge>
<Badge variant="danger" dot>Live</Badge>
```

Variants map to the semantic tokens: `neutral`, `accent`, `success`, `warning`, `danger`, `info`.
