Placeholder shown when a list/table/page has no data yet.

```jsx
<EmptyState
  title="No pools yet"
  description="Create your first pool to start inviting friends."
  action={<Button variant="primary">Create pool</Button>}
/>
```
