Compact metric tile for dashboards — label, a big number, an optional trend delta, and a hint line. Unifies the "metric card" / "KPI tile" pattern seen across all three source apps.

```jsx
<StatTile label="Active pools" value="128" delta="+12%" direction="up" hint="vs. last week" tone="accent" />
```
